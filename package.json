{
  "name": "early-eagles-worker",
  "version": "1.0.0",
  "description": "Early Eagles 🦅 — CF Worker auth signer",
  "main": "src/index.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@noble/curves": "^1.4.0",
    "@noble/hashes": "^1.4.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
